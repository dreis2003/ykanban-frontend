# Copie para .env (ou .env.development / .env.production) e ajuste conforme o ambiente.
# Nunca commitar .env real. Toda variável VITE_* é embutida no bundle e fica
# visível no navegador — nunca coloque segredos aqui.

VITE_API_BASE_URL=http://localhost:8080/api/v1
